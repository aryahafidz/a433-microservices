version: "3.2"
services:
  rabbitmq:
    image: rabbitmq:3.11-management 
    container_name: "rabbitmq_container_shipping_service"
    ports:
      - 3001:3001
    volumes:
      - ~/.docker-conf/rabbitmq/data/:/var/lib/rabbitmq/
      - ~/.docker-conf/rabbitmq/log/:/var/log/rabbitmq
    networks:
      - app_network

  shipping-service:
    image: ghcr.io/aryahafidz/shipping-service:latest
    depends_on:
      - "rabbitmq"
    command: sh -c '/bin/wait-for-it.sh rabbitmq:5672 --timeout=30 -- node index.js'
    environment:
      AMQP_URL: amqp://guest:guest@rabbitmq:5672
    networks:
      - app_network

networks:
  app_network:
    driver: bridge